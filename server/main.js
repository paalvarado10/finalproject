import { Meteor } from 'meteor/meteor';
import '../imports/api/producciones.js';
import '../imports/api/aplicaciones.js';
import '../imports/api/artistas.js';

Meteor.startup(() => {
  // code to run on server at startup
});
